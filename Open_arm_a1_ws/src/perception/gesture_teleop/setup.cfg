[develop]
script_dir=$base/lib/gesture_teleop
[install]
install_scripts=$base/lib/gesture_teleop

[tool:pytest]
testpaths = test
markers =
    integration: exercises multiple modules together (still no hardware/network)
    ros: requires rclpy to be importable; skipped automatically otherwise
    slow: property-based tests with a larger-than-default example budget
